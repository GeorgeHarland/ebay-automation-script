# Ebay automation script

This script will get all of a user's active listings and reduce the price to 98% of the original.

### Running

First get an ebay user token from their developer portal (oauth) 

Then run:

```
npm i dotenv fast-xml-parser

node index.js
```